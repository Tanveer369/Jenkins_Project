@Library("checkoutgit") _

pipeline {
    agent any
    stages {
        stage('Code Checkout') {
            steps {
                script {
                  def workingDir = checkoutCode("https://github.com/Tanveer369/game.git", "main")
                  echo $workingDir
                }
            }
        }
    }
}
